{
  "name": "cats-screening",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n shared,cv,api,web -c \"magenta,blue,green,cyan\" \"npm run dev --workspace packages/shared\" \"npm run dev --workspace packages/cv-core\" \"npm run dev --workspace apps/api\" \"npm run dev --workspace apps/web\"",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "dataset:manifest": "tsx scripts/dataset/build-manifest.ts",
    "pipeline:calibrate": "tsx scripts/pipeline/build-calibration.ts",
    "pipeline:eval": "tsx scripts/pipeline/eval.ts",
    "build": "npm run build --workspace packages/shared && npm run build --workspace packages/cv-core && npm run build --workspace apps/api && npm run build --workspace apps/web",
    "typecheck": "npm run build --workspace packages/shared && npm run build --workspace packages/cv-core && npm run typecheck --workspace packages/shared && npm run typecheck --workspace packages/cv-core && npm run typecheck --workspace apps/api && npm run typecheck --workspace apps/web && tsc -p tsconfig.vercel.json --noEmit",
    "format": "prettier -w .",
    "format:check": "prettier -c ."
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

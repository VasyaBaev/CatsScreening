# Step 172 — Публикация очистки локальных путей

Дата: 2026-09-04

RESULT: Git-отслеживаемые изменения переноса каталогов опубликованы в GitHub-safe ветке.

IN SCOPE: актуальные абсолютные пути Capture, правила игнорирования локальных worktree, scoped-коммиты и проверка push.

OUT OF SCOPE: незавершённые runtime/data/research-файлы, merge веток и переписывание старой истории.

MODE: predeploy

## Сделано

- Локальный pnpm-store удалён как воспроизводимый кэш.
- Чистый baseline-worktree удалён с сохранением его ветки и commit.
- Нужный hotfix-safe worktree перенесён в `D:\dev\CatsScreening\CatsScreening-hotfix-safe` с сохранением Git metadata и незавершённых пользовательских документов.
- Generated `node_modules`, `dist` и `.tmp/deploy` удалены из перенесённого worktree.
- В актуальных Capture V2/V10 prompt-файлах исправлены абсолютные пути.
- В `.gitignore` добавлены корневые каталоги локальных worktree.
- Предсуществующие незавершённые файлы не индексировались.

## Публикация и проверки

- `codex/capture-hotfix-mobile-first` перед изменениями имела divergence `0/0` относительно `origin/codex/capture-hotfix-mobile-first`.
- Коммит `fbca6a17f084d5c0f2c4024e8d441423ba0be109` опубликован обычным push; локальный и удалённый SHA совпали.
- Финальный commit добавляет `.gitignore` и этот отчёт в ту же GitHub-safe ветку.
- Проверены точные staged-пути, `git diff --cached --check`, отсутствие старых активных ссылок и итоговое совпадение локального/remote SHA.

## Почему не публикуется основная история

Локальная `codex/production-like-capture-site` не имеет remote-ветки и содержит исторический blob размером 154 532 822 байта (147,37 МиБ). Обычный GitHub push этой истории невозможен, а её переписывание не относится к текущей уборке и рискованно при наличии незавершённых пользовательских изменений. Поэтому полный эквивалент результата публикуется через ранее созданную GitHub-safe ветку без force-push.

## Результат

GitHub-safe ветка содержит актуальные пути, правила игнорирования и отчёт. Локальные кэши и generated-файлы не попали в Git; пользовательские незавершённые материалы сохранены вне staged scope.
